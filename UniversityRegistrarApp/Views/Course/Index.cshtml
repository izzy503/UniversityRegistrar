

@using UniversityRegistrar.Models;

<h1>Course Listing</h1>

@if(@Model.Count == 0)
{
  <h3>No courses have been posted</h3>
}
else
{
  <ul>
  @foreach (Course course in Model)
  {
    <li>@Html.ActionLink($"{course.Name}", "Details", new { id = course;.CourseID }>)</li>
  }
  </ul>
}
